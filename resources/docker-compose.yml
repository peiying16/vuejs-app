version: "2"

services:

  nginx:
    build: nginx
    volumes:
      - ../:/srv/app
    ports:
      - 8088:80

  app:
    build: ./app
    volumes:
      - ../:/srv/app
    command: ["node", "/srv/app/server/index.js"]
